# Admin Backend

This folder is a placeholder for admin backend services (APIs, server functions).

Currently this project is a frontend-only demo. Move or add server code here (Express, Fastify, serverless functions, etc.) if you plan to implement a real backend for admin authentication and article management.
